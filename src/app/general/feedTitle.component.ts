import { Component } from '@angular/core';

@Component({
    selector: 'feed-title',
    template : `
        <section class="content-header">
            <h1> Current statistics / total feeds <span class="">83</span> </h1>
        </section>
    `
})

export class FeedTitle {

}